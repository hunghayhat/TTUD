// Include all library
#include <bits/stdc++.h>
// Include all namespace
using namespace std;
// Define some common use constants
#define ll long long
#define ld long double
#define PI 3.1415926535897932384626433832795
#define MOD 1000000007
// Define some common use functions

int main(){
    // Optimize input and output
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    // Input
    // Read whole line
    string s;
    getline(cin, s);
    // Read if the amount of input is unknown
    while(cin >> s){
        // Code here
    }
    // Using file input and output
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    // Code here
    cout << "\n"; // Print a new line using "\n" instead of endl (endl flushes the buffer)
    return 0;
}
